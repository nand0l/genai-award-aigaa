version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "🔧 Installing Node.js 20.19.0"
        - nvm install 20.19.0
        - nvm use 20.19.0
        - node -v
        - npm ci --cache .npm --prefer-offline
    build:
      commands:
        - echo "🚀 Starting production build with Vite"
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .npm/**/*
